module DoubleBuffering;

global protocol DoubleBuffering (role S, role K, role T) {
	rec loop {
		ready() from K To S;
		copy() from S To K;
		ready() from T To K;
		copy() from K To T;
		do DoubleBuffering(S,K,T);
	}
}
